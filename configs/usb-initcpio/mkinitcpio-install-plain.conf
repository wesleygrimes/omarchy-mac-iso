# Initramfs for an unencrypted installed root. Same Type-C modules as the
# LUKS install image, without the cryptsetup hook (that prints a spurious
# prompt/error when there is no cryptdevice).

MODULES=(
  apple-tunable
  phy_apple_atc
  mux_apple_display_crossbar
  typec
  tps6598x_core
  tps6598x
  sn201202x?
  dwc3
  dwc3_apple
  xhci_plat_hcd
  xhci_hcd
  usb_storage
  uas
  nvme-apple?
  nvme?
  appledrm
  btrfs
  nls_iso8859_1
)

BINARIES=()

FILES=()

HOOKS=(base asahi udev kms keyboard plymouth omarchy-usb-wait filesystems)

COMPRESSION="zstd"
COMPRESSION_OPTIONS=(-T0 -3)
