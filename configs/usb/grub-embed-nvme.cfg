# Embedded only in BOOTAA64-NVME.EFI. The marker and config names are
# deliberately distinct from the USB live environment so GRUB cannot select
# the wrong ESP when a USB stick is attached during an NVMe install.
search --no-floppy --file /omarchy-nvme-live --set=root
configfile /grub/grub-nvme.cfg
